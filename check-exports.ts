import * as contracts from '@midnight-ntwrk/midnight-js-contracts';
import * as walletSdk from '@midnight-ntwrk/wallet-sdk';
import * as protocol from '@midnight-ntwrk/midnight-js-protocol';
import * as utils from '@midnight-ntwrk/midnight-js-utils';

console.log('Contracts:', Object.keys(contracts));
console.log('WalletSDK:', Object.keys(walletSdk));
console.log('Protocol:', Object.keys(protocol));
console.log('Utils:', Object.keys(utils));
