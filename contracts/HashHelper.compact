pragma language_version >= 0.14.0;
import CompactStandardLibrary;

export circuit hash_pair(left: Bytes<32>, right: Bytes<32>): Bytes<32> {
  return persistentHash<Vector<2, Bytes<32>>>([left, right]);
}
